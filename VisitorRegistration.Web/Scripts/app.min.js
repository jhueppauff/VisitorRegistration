function resizeCanvas(){var n=Math.max(window.devicePixelRatio||1,1);canvas.width=canvas.offsetWidth*n;canvas.height=canvas.offsetHeight*n;canvas.getContext("2d").scale(n,n)}function save(){signaturePad.isEmpty()?alert("Please provide signature first."):($("#signaturebinary").val(signaturePad.toDataURL()),$("#hiddenbutton").click())}var wrapper=document.getElementById("signature-pad"),clearButton=wrapper.querySelector("[data-action=clear]"),saveButton=wrapper.querySelector("[data-action=save]"),canvas=wrapper.querySelector("canvas"),signaturePad;window.onresize=resizeCanvas;resizeCanvas();signaturePad=new SignaturePad(canvas);clearButton.addEventListener("click",function(){signaturePad.clear()});